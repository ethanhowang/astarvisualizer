import pygame
import math
from queue import PriorityQueue

WIDTH = 800
WIN  = pygame.display.set_mode((WIDTH, WIDTH)) # setting the display size of the pygame
pygame.display.set_caption("A* Path Finding Algorithm") # creating a caption
